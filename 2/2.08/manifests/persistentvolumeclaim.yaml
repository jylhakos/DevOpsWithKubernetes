apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  namespace: todos-namespace
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi