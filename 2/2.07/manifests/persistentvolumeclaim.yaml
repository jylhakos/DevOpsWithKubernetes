apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  namespace: app-db-namespace
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi