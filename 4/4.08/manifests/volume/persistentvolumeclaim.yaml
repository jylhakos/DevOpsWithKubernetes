apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  #namespace: todos-db-namespace
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi